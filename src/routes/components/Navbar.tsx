

export const Navbar = () => {
    return <nav>
        <ul>
            {/* <li><NavLink className={({ isActive }) => isActive ? 'nav-active' : ''} to="/home">Home</NavLink></li> */}
        </ul>
    </nav>;
};
